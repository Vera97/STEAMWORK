const state = {
    stuList: [],
    courseId: '',        /* the id of the course being display. */
    classId: '',
    periodsList: [],
    relatedList: []
};

const mutations = {};

const getters = {};

const actions = {};


export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}
